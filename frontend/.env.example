# URL base de la API Django (solo necesaria en producción cuando el backend
# está en un host diferente al frontend de Netlify).
# En desarrollo, Vite ya hace proxy de /api a localhost:8000 — no hace falta.
VITE_API_URL=https://tu-backend.railway.app
